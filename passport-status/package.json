{
  "name": "passport-status",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ passport-status",
    "create-local": "graph create --node http://localhost:8020/ passport-status",
    "remove-local": "graph remove --node http://localhost:8020/ passport-status",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 passport-status",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.64.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
